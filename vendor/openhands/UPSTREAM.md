# OpenHands vendored snapshot metadata

- Upstream repository: `OpenHands/OpenHands` (inferred from vendored project layout; exact remote URL not embedded in this snapshot).
- Upstream release/tag: unknown.
- Upstream commit SHA: unknown.
- Import date: manually vendored (date not embedded in snapshot metadata).

## Local TeleClaw note

This directory is a vendored source snapshot inside TeleClaw.

- Do not treat it as a git submodule.
- Avoid casual edits inside `vendor/openhands`.
- Prefer adapting behavior from TeleClaw integration code under `src/teleclaw/worker/openhands`.
